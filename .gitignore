node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.venv/
.env*
!.env.example
# Local-only deployment inputs. These stay out of Git even when Cloud Run
# intentionally consumes them through `.gcloudignore`/`.dockerignore`.
cloudrun.env
serviceAccountKey.json
