/**
 * Compatibility re-export.
 * --------------------------------------------------------------------------
 * The assessment landing workflow now lives under `src/features/assessment-tool`.
 * Keep this file so existing imports continue to work during the gradual
 * feature-first migration.
 */
export { Dashboard } from '../features/assessment-tool/pages/AssessmentDashboard';
